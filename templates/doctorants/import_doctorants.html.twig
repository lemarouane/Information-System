{% extends 'template.html.twig' %}

{% block title %}Importer des Doctorants{% endblock %}

{% block body %}
<div class="page-breadcrumb d-none d-sm-flex align-items-center mb-3">
    <div class="breadcrumb-title pe-3">Importer des Doctorants</div>
</div>
<div class="row">
    <div class="col-lg-12 mx-auto">
        <div class="card shadow-sm border-0">
            <div class="card-body">
                <h5 class="card-title">Importer des Doctorants depuis Excel</h5>
                <hr>
                {% include "flaches.html.twig" %}
                
                <div class="alert alert-info">
                    <h6>Instructions:</h6>
                    <p>Le fichier Excel doit contenir les colonnes suivantes dans cet ordre:</p>
                    <ol>
                        <li>E-mail Institutionnel (A1)</li>
                        <li>Code Apogee (B1)</li>
                        <li>Nom (C1)</li>
                        <li>Prenom (D1)</li>
                    </ol>
                </div>

                <form method="post" enctype="multipart/form-data" class="mt-4">
                    <div class="mb-3">
                        <label for="excel_file" class="form-label">Fichier Excel</label>
                        <input type="file" 
                               class="form-control" 
                               id="excel_file" 
                               name="excel_file" 
                               accept=".xlsx,.xls"
                               required>
                    </div>

                    <div class="text-end mt-3">
                        <button type="submit" class="btn btn-primary">Importer</button>
                        <a href="{{ path('list_doctorants') }}" class="btn btn-secondary">Retour</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}